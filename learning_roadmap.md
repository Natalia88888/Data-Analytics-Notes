# 🗺️ ПУТЬ АНАЛИТИКА ДАННЫХ: ОТ НОВИЧКА ДО PRO

## ЭТАП 1. SQL — Язык общения с базами данных
*Основа основ. Без этого невозможно достать данные для анализа.*

*   **База:** `SELECT`, `FROM`, `WHERE`, `ORDER BY`, `LIMIT`.
*   **Агрегация:** `GROUP BY`, `HAVING`, функции `SUM`, `AVG`, `COUNT`, `MIN/MAX`.
*   **Связи:** `JOIN` (Inner, Left, Right, Full) — как склеивать таблицы.
*   **Продвинутый уровень:** 
    *   `CTE` (WITH ...) — для чистоты кода.
    *   `Window Functions` (OVER, PARTITION BY) — для рейтингов и накопительных итогов.

---

## ЭТАП 2. Python для анализа данных (Pandas)
*Инструмент для глубокой очистки и трансформации данных.*

*   **Библиотека Pandas:**
    *   `DataFrame` и `Series` (структура таблиц).
    *   Загрузка данных (`pd.read_csv`, `pd.read_excel`).
    *   Фильтрация и условия (`df[(условие) & (условие)]`).
    *   Группировка и агрегация (`.groupby().agg()`).
    *   Объединение таблиц (`.merge()`, `.concat()`).
    *   Очистка данных (обработка пропусков `NaN`, изменение типов данных).

---

## ЭТАП 3. Визуализация данных (Storytelling)
*Превращаем скучные таблицы в понятные отчеты.*

*   **Инструменты:** `Matplotlib` (база) и `Seaborn` (красота).
*   **Типы графиков:**
    *   `Line Plot` — динамика во времени.
    *   `Bar Chart` — сравнение категорий.
    *   `Histogram` — распределение (частота).
    *   `Scatter Plot` — поиск зависимостей.
    *   `Boxplot` — поиск аномалий (выбросов).

---

## ЭТАП 4. Математика и Статистика
*Чтобы не делать ложных выводов из данных.*

*   **Описательная статистика:** Среднее, медиана, мода, стандартное отклонение.
*   **Вероятность:** Как понять, что событие не случайно.
*   **A/B тесты:** Проверка гипотез (стало лучше или нам кажется?).

---

## ЭТАП 5. BI-инструменты и Автоматизация
*Финальный штрих для работы в компании.*

*   **BI-системы:** Power BI, Tableau или Superset (создание интерактивных дашбордов).
*   **Инструменты:** Git (контроль версий), работа с API (выгрузка данных из интернета).
